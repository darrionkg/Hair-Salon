<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>Salon's Stylists</title>
  <link rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css'>
  <link rel='stylesheet' href='~/css/styles.css' type="text/css">
</head>
<body>
  <div class="container">
    <div class="jumbotron">
      <h1>Stylists</h1>
    </div>
    <ul>
    @if (Model.Count == 0)
    {
      <p>No stylists have been added yet!</p>
    }
    @if (Model.Count != 0)
    {
      @foreach (var stylist in Model)
      {
        <a href="/stylists/@stylist.GetId()">Name: @stylist.GetName()</a><br>
      }
    }
    </ul>
    <a href="/stylists/new" class="btn btn-dark">Add Stylist</a>
    <a href="/" class="btn btn-dark">Back to homepage</a>
  </div>
</body>
</html>
